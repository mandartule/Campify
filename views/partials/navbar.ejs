<nav class="navbar navbar-expand-lg fixed-top modern-navbar">
  <div class="container">
    <!-- Brand/Logo -->
    <a class="navbar-brand modern-brand" href="/">
      <i class="fas fa-mountain me-2"></i>
      <span>Campify</span>
    </a>

    <!-- Search Bar (visible on larger screens) -->
    <div class="navbar-search d-none d-lg-flex">
      <div class="search-container">
        <div class="input-group">
          <input type="text" class="form-control search-input" placeholder="Search campgrounds..."
           value="<%= typeof search !== 'undefined' ? search : '' %>">
          <button class="btn search-btn" type="button">
            <i class="fas fa-search"></i>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile Toggle Button -->
    <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="custom-toggler-icon"></span>
      <span class="custom-toggler-icon"></span>
      <span class="custom-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Main Navigation Links -->
      <div class="navbar-nav me-auto">
        <a class="nav-link modern-nav-link" href="/campgrounds">
          <i class="fas fa-campground me-1"></i>
          Explore
        </a>
        <a class="nav-link modern-nav-link" href="/campgrounds/new">
          <i class="fas fa-plus-circle me-1"></i>
          Add Campground
        </a>
      </div>

      <!-- User Authentication Links -->
      <div class="navbar-nav">
        <% if(!currentUser) { %>
          <a class="nav-link modern-nav-link" href="/login">
            <i class="fas fa-sign-in-alt me-1"></i>
            Login
          </a>
          <a class="nav-link modern-nav-link btn-register" href="/register">
            <i class="fas fa-user-plus me-1"></i>
            Register
          </a>
          <% } else { %>
            <div class="nav-item dropdown">
              <a class="nav-link dropdown-toggle modern-nav-link" href="#" role="button" data-bs-toggle="dropdown">
                <i class="fas fa-user-circle me-1"></i>
                Profile
              </a>
              <ul class="dropdown-menu modern-dropdown">
                <li><a class="dropdown-item" href="#"><i class="fas fa-user me-2"></i>My Profile</a></li>
                <li><a class="dropdown-item" href="#"><i class="fas fa-campground me-2"></i>My Campgrounds</a></li>
                <li>
                  <hr class="dropdown-divider">
                </li>
                <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
              </ul>
            </div>
            <% } %>
      </div>
    </div>
  </div>
</nav>